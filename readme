Crear el entorno virtual
    en linux:
        sudo apt install python3.10-venv
        python3 -m venv env
        source env/bin/activate

Instalar requirements
    pip install -r requirements.txt

crear las variables de entorno en el archivo GUTSOFT/.env

    El contenido del .env puede algo como:
        
        SECRET_KEY=django-insecure-zq_r(ks0dfgsdfgosdfg!byk6nx2gaizuv^^^^55%7@=0s7ny*q#h^@lzov67
        DEBUG=True
        ALLOWED_HOSTS=127.0.0.1,localhost
        EMAIL_HOST_PASSWORD=Mipass123*
        DBUSER='root'
        DBPASS=''

crear las migraciones
    python manage.py makemigrations
    python manage.py migrate

Crear el superusuario
    python manage.py createsuperuser

Ejecutar el servidor
    python manage.py runserver


ejcutar la ruta
    127.0.0.1:8000/inicializar
    para precargar registros a la base de datos.

